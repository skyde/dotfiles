# Prefer VS Code for text; fallback to Neovim
mime ^text,  label code,  =code -r -- "$@"
mime ^text,  label nvim,  =nvim -- "$@"

# Images and PDFs via default macOS app
mime ^image, label open,  =open -- "$@"
ext pdf,     label open,  =open -- "$@"
