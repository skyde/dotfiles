# Basic Yazi configuration
# Schema: https://yazi-rs.github.io/schemas/yazi.json

[mgr]
ratio = [1, 4, 7]
show_hidden = true
sort_by = "mtime"
sort_reverse = true

[preview]
wrap = "yes"
image_delay = 0

# Yazi clears an asynchronous preview before its replacement is ready. Use a
# bounded synchronous renderer for text so cursor moves commit atomically and
# large files cannot leave the preview pane empty between frames.
[[plugin.prepend_previewers]]
mime = "text/*"
run = "sync-text-preview"

[[plugin.prepend_previewers]]
mime = "application/{json,xml,javascript,x-javascript,x-sh,x-shellscript,toml,x-yaml,x-toml}"
run = "sync-text-preview"

[[plugin.prepend_previewers]]
name = "*.{txt,log,csv,tsv,zsh,zshrc,bashrc,sh,conf,cfg,ini,lua,rs,go,py,rb,ts,tsx,js,jsx,cs,cpp,hpp,cc,h,md,xml,yml,yaml,toml,json}"
run = "sync-text-preview"

# Preview code through bat so the right-hand pane uses the same syntax theme
# (BAT_THEME) and the same custom syntaxes as bat, delta and VS Code.
# See plugins/bat-preview.yazi/main.lua for why this beats mgr.syntect_theme.
[[plugin.prepend_previewers]]
mime = "text/*"
run = "bat-preview"

[[plugin.prepend_previewers]]
mime = "application/{json,xml,javascript,x-javascript,x-sh,x-shellscript,toml,x-yaml,x-toml}"
run = "bat-preview"

# Extensionless dotfiles and anything `file` reports as generic data but that
# is really text (.zshrc, Dockerfile, Makefile, ...).
[[plugin.prepend_previewers]]
name = "*.{zsh,zshrc,bashrc,conf,cfg,ini,lua,rs,go,py,rb,ts,tsx,js,jsx,cs,cpp,hpp,cc,h,md,yml,yaml,toml,json}"
run = "bat-preview"

# [[plugin.prepend_fetchers]]
# id = "git"
# name = "*"
# run = "git"
#
# [[plugin.prepend_fetchers]]
# id = "git"
# name = "*/"
# run = "git"
