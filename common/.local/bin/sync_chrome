#!/usr/bin/env bash
set -euo pipefail

# Navigate to Chrome directory
cd ~/chrome/src/

# Update
git rebase-update
gclient sync -D

gn gen out/Default

# Rebuild zoekt index
si

# Build Chrome
autoninja -C out/Default
